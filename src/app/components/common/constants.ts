// API routes
export const ROUTES = {
  HOME: '/',
  LISTINGS: '/listings'
}

// API routes
export const API_ROUTES = {
  REGISTER: '/api/register',
  LISTINGS: '/api/listings',
  FAVORITES: '/api/favorites'
}